<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
    
    </head>
    <body>
        <div class="container">
            <div class="row">
                <form method="POST" action="/atualizar/{{cliente.dataValues.id}}">

                    <h4>Cadastrar - Agendamento de Pontenciais Clientes</h2><br>
                    <h6>Sistema utilizado para agendamento de serviços.</p> 
                    <br> 

                    <!-- campo nome -->
                    <div class="mb-3">
                        <label for="nome" class="form-label">Nome:</label>
                        <input name="nome" type="text" class="form-control" id="nome" required placeholder="Insira seu nome" value="{{cliente.dataValues.nome}}">
                    </div>

                    <!-- campo telefone -->
                    <div class="mb-3">
                        <label for="txtTel" class="form-label">Telefone:</label>
                        <input name="telefone" type="tel" class="form-control" id="telefone" required placeholder="(xx) xxxxx-xxxx" value="{{cliente.dataValues.telefone}}">
                    </div>

                    <!-- campo origem -->
                    <div class="mb-3">
                        <label for="Origem" class="form-label">Origem: </label>
                        <input type="text" class="form-control" name="origem" id="origem" value="{{cliente.dataValues.origem}}">
                    </div>

                    <br>

                    <!-- campo data ctt -->
                    <div class="mb-3">
                        <label for="txtDtContato">Data do Contato:</label>
                        <input name="data" type="date" class="form-control" id="data" required name="data_contato" value="{{cliente.dataValues.data_contato}}">
                    </div>

                    <!-- campo observacao -->
                    <div class="mb-3">
                        <label for="txtObs" id="inputField" class="form-label">Observação:</label>
                        <textarea class="form-control" name="observacao" id="observacao" rows="3">{{cliente.dataValues.observacao}}</textarea>
                    </div>

                    <!-- button cadastrar -->
                    <button type="submit" id="btnCadastrar" class="btn btn-primary">Cadastrar</button>
                </form>
            </div>
        </div>
    </body>
</html>